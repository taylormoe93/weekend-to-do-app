//CLIENT.JS

$( document ).ready( onReady );

function onReady(){
    //event listeners 
    $( '#addTaskButton' ).on( 'click', addTask ); //links to addTask function
    // taskComplete button
    // taskDelete button

    //getTasks();
}